```bash
readonly VARIABLE="banana" #When a variable is set to constant and should not be changed, make the variable all caps

export VARIABLE #For scripts that will be used outside of the existing script
```